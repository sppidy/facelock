# Display only: PAM success remains the sole unlock decision.
label {
    monitor =
    text = cmd[update:500] facelock-feedback --text
    font_size = 12
    position = 0, -240
    halign = center
    valign = center
}
