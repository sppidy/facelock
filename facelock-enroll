#!/usr/bin/python3 -I
"""Open the GTK enrollment wizard."""
from pathlib import Path
import sys

here = Path(__file__).resolve().parent
sys.path.insert(0, str(here if (here / 'facelock').is_dir() else here.parent / 'lib/facelock'))
from facelock.gui import main

main()
