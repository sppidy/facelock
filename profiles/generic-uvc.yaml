# Explicit RGB-only USB webcam profile. No active IR check.
auth:
  required_sensors: [rgb]
cameras:
  rgb: uvc:/dev/video0
capture:
  mode: uvc
models:
  dir: /usr/share/facelock/models
store:
  dir: /var/lib/facelock
pam:
  user: ''
